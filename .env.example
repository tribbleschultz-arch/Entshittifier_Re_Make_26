# Diese App benötigt keine Umgebungsvariablen und keine externen API-Keys.
# Alle Daten (Fragen, Gewichtungen, Zonen) liegen als statische JSON-Dateien
# unter `public/` vor und werden lokal im Browser verarbeitet.
#
# Es werden bewusst KEINE KI-Dienste oder externen Endpunkte angebunden.
